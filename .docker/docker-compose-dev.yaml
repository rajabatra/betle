name: betl-dev
services:
  backend:
    build:
      context: ../backend
    command: npm start
    develop:
      watch:
        - action: sync
          path: ../backend
    environment:
      - LOCAL_DB=true
      - DB_PASS=mysqlrootpass
      - DB_NAME=betldb
    networks:
      - client-side
      - server-side
    depends_on:
      - db
    ports:
      - 8081:8081

  frontend:
    build:
      context: ../frontend
      target: build
    develop:
      watch:
        - action: sync
          path: ../frontend/
    networks:
      - client-side
    ports:
      - 3000:3000
    command: npm start
    depends_on:
      - backend

  db:
    image: mysql:8
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=mysqlrootpass
      - MYSQL_DATABASE=betldb
    volumes:
      - db-data:/var/lib/mysql
    ports:
      - 3306:3306

networks:
  client-side: {}
  server-side: {}
volumes:
  db-data: {}
